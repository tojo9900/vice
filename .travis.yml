language: c++

before_install:
 - sudo apt-get install autoconf autotools-dev bison bison++ debhelper dpkg-dev flex libasound2-dev libavformat-dev libgtk2.0-dev libgtkglext1-dev libieee1284-3-dev libjpeg-dev libpng-dev libpulse-dev libreadline-dev libswscale-dev libtwolame-dev libvte-dev xfonts-utils

install:
 - ./autogen.sh
 - ./configure --enable-gnomeui --enable-ethernet --enable-fullscreen
 - make
